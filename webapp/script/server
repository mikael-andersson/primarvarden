PROJECT_ROOT=`dirname $0`/../
if [ "$1" = "home" ]
   then
   CORE="--core $HOME/lib/sbcl/sbcl.core"
fi
SWANK_PORT=4005
WEBLOCKS_PORT=8080
echo "Project root: $PROJECT_ROOT"
sbcl $CORE --userinit $PROJECT_ROOT/primarvarden.sbclrc $PROJECT_ROOT $WEBLOCKS_PORT $SWANK_PORT --no-linedit
